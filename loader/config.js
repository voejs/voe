export default {
  caseStyle: 'camel',
  lowercaseFirst: true,
  initializer: null,
  call: true,
  inject: null, // 一般指 `app` 对象
  target: null, // inContext 模式下指 app.controller等 其他都指app
  override: false,
  inContext: false,
  runtime: null
}